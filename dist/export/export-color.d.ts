export declare function resolveExportColor(css: string): string;
/** ExcelJS wants an 8-digit ARGB hex (alpha first) with no "#" for Font.color/Fill colors. */
export declare function toArgb(css: string): string;
